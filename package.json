{
  "name": "ucab-entrevistas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "npm start --prefix server"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^173.0.0",
    "mysql2": "^3.22.6",
    "nanoid": "^5.0.7",
    "nodemailer": "^9.0.3",
    "resend": "^4.0.0"
  }
}
